spring:
  application:
    name: eureka-server
server:
  port: 8761
eureka:
  instance:
    hostname: "${HOSTNAME}.eureka"
  client:
    register-with-eureka: false
    fetch-registry: false
  serviceUrl:
    defaultZone: ${EUREKA_URI}
management:
  tracing:
    sampling:
      probability: 1.0
