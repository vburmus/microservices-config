spring:
  application:
    name: eureka-server
  cloud:
    zipkin:
      base-url: ${ZIPKIN_URI}
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
management:
  tracing:
    sampling:
      probability: 1.0